package com.sunsekey.algorithm;

/**
 * 替换字符串中的空格，将一个字符串中的空格替换成“%20”。例如：当字符串为 We Are Happy.则经过替换之后的字符串为 We%20Are%20Happy。
 */
public class ReplaceWhiteSpace {

    /**
     * 循环处理，没什么特别
     * @param sb
     * @return
     */
    public String replace(StringBuilder sb){
        return "";
    }
}
